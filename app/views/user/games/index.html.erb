<% @games.each do |game| %>
<div>
  <%= attachment_image_tag game, :image %>
  <p>投稿ユーザー画像：<%= image_tag('no_image.jpg') %></p>
  <p>ショップ名：<%= game.title %></p>
  <p>説明：<%= game.impressions %></p>
  <p>ユーザーネーム：<%= game.user.name %></p>
</div>
<% end %>